<!DOCTYPE html>
<html>
  <head>
      <% include ./partials/headers.ejs %>

     
  </head>
  <body>
      <%- include(navbar) %>
    <div class="container-fluid pt-5 h-100 position-absolute">
      <%- include(page) %>
    </div>
    <script>
        $(document).ready(function() {
          $('a.active').removeClass('active');
          $('a[href="' + location.pathname + '"]').addClass('active'); 
        });
      </script>
  </body>
</html>
